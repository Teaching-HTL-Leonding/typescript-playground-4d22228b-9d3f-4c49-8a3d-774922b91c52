function setup() {
    createCanvas(400, 200);
    background("black");
    noStroke();

    // <<< Add your code here
}

// <<< Add the function `moveClicked` with the required code here
function mouseClicked() {

    let message: string

    if  (mouseX < width / 3) {
    message = "yellow" }

    }
}